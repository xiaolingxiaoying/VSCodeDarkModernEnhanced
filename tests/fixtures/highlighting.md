# Dark Modern Enhanced

> A block quote with **bold**, *italic*, ~~deleted~~ and [a link](https://example.com).

| Language | Enabled |
| --- | --- |
| Python | yes |

```python
class Greeter:
    def greet(self, name: str) -> str:
        return f"Hello, {name}"
```

```typescript
interface User { readonly name: string }
const greet = (user: User): string => `Hello, ${user.name}`;
```
